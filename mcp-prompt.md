# Metadata MCP PoC 現状分析・アーキテクチャ検討プロンプト

あなたは、GitHub Copilot / MCP（Model Context Protocol）/ Java / Oracle / エンタープライズシステム開発に詳しいシニアソフトウェアアーキテクトとして、このプロジェクトを分析してください。

現時点では、すぐにコードを修正・追加することを目的としていません。

まず、このリポジトリに存在する現在のMCP実装を確認し、

1. 現在どのような構成になっているか
2. 何ができる状態なのか
3. 本来目指している構成とどこが一致しているか
4. どこにズレ・不足・過剰設計があるか
5. 今後どの方向へ進めるのがよいか

を整理してください。

---

# 1. 背景

この開発現場では、一般的なJavaアプリケーションとは異なり、Metadata-Driven Architectureに近い仕組みを利用しています。

Javaにはあらかじめ、

- 共通処理
- クラス
- メソッド
- 業務処理部品
- Metadataを解釈する処理

などが実装されています。

一方、Oracle Databaseには、アプリケーションの振る舞いを定義するMetadataが格納されています。

Metadataには、たとえば以下のような情報があります。

- 処理ルール
- 処理順序
- 条件
- パラメータ
- Javaクラスとの対応
- Javaメソッドとの対応
- コード値
- 各種定義
- Metadata同士の関連

そのため、

「Javaソースコードだけを見る」

だけではアプリケーション全体の動作を理解することができません。

実際のアプリケーションの振る舞いは、おおむね

```text
Javaの実装
+
Oracle上のMetadata
=
実際のアプリケーション動作
```

という関係になっています。

---

# 2. 現在の課題

開発ではVS CodeとGitHub Copilotを利用しようとしています。

しかしGitHub Copilotは通常、

- 開いているソースコード
- リポジトリ内のファイル
- 開発者がチャットで与えた情報

などを中心に判断します。

Oracle Database上に存在するMetadataを知らないため、この開発現場固有の仕組みを十分理解できません。

その結果、

- Javaコードの意味は説明できる
- 一般的なコード修正はできる

一方で、

- このJavaメソッドがどのMetadataから利用されているか分からない
- Metadataを含めた影響範囲を判断できない
- 実際のアプリケーション動作を正しく理解できない
- Metadataの作成・変更方法を判断できない
- 障害調査時にJavaとMetadataを横断して調査できない

という問題があります。

この状態ではGitHub Copilotを十分な開発支援AIとして利用できません。

---

# 3. 今回のMCP構想

この問題を解決するため、

```text
GitHub Copilot
        ↓
      MCP
        ↓
Oracle Metadata Database
```

という仕組みを検討しています。

ただし、目的は

「CopilotからOracleへ自由にSQLを実行できるようにすること」

ではありません。

目指しているのは、

**GitHub Copilotが必要なときに、この開発現場固有のMetadataを安全かつ適切な粒度で取得できる仕組み**

です。

MCP Serverは単なるDB接続Proxyではなく、

**GitHub CopilotとMetadataの間に存在する、開発知識アクセス用のGateway**

として位置付けたいと考えています。

概念的には以下です。

```text
┌─────────────────────────┐
│ Developer                │
│ VS Code                  │
│ GitHub Copilot           │
└────────────┬────────────┘
             │
             │ MCP
             ▼
┌─────────────────────────┐
│ Metadata MCP Server      │
│                         │
│ ・Metadata検索           │
│ ・Metadata取得           │
│ ・Javaとの関連取得       │
│ ・依存関係取得           │
│ ・アクセス制御           │
│ ・結果整形               │
│ ・監査                   │
└────────────┬────────────┘
             │
             │ Read Only
             ▼
┌─────────────────────────┐
│ Oracle Database          │
│                         │
│ Metadata Tables          │
└─────────────────────────┘
```

これは現時点での仮説であり、現在の実装を確認した結果、より適切な構成があれば提案してください。

---

# 4. 最終的に目指している状態

最終目標は、

**GitHub CopilotがJavaコードとMetadataの両方を理解し、この開発現場固有のコンテキストを踏まえて開発支援できる状態**

です。

たとえば開発者が、

```text
このメソッドを変更した場合の影響範囲を調査してください。
```

と依頼した場合、

Copilotが

```text
Javaコード調査
       +
Metadata調査
       ↓
総合的な影響分析
```

を行える状態を目指します。

将来的には以下のような利用を想定しています。

### コード理解

```text
このJavaクラスの役割を説明してください。
Metadataとの関連も含めて説明してください。
```

### Metadata逆引き

```text
このJavaメソッドを使用しているMetadataを調査してください。
```

### Metadata理解

```text
このMetadata IDがどのような処理を実現しているか説明してください。
```

### 影響調査

```text
このJavaメソッドを変更した場合、
影響を受けるJavaコードとMetadataを調査してください。
```

### 障害調査

```text
このエラーが発生する可能性がある
Java処理とMetadataを調査してください。
```

### 開発支援

```text
この仕様を実現するには、
JavaとMetadataのどちらを変更するべきか調査してください。
```

### Metadata作成支援

```text
類似する既存Metadataを調査し、
今回作成すべきMetadata案を提示してください。
```

最終的には、

```text
Source Code
     +
Metadata
     +
開発ルール・設計知識
     ↓
GitHub Copilot
     ↓
このシステムを理解した開発支援
```

という状態を目指します。

---

# 5. 重要な設計方針

以下は現時点で重要だと考えている方針です。

ただし、現在の実装を確認したうえで問題があれば指摘してください。

## 5.1 Read Onlyを基本とする

PoCおよび初期運用では、MCP経由でOracle Databaseを更新しません。

原則として、

```text
SELECT / Metadata参照のみ
```

とします。

---

## 5.2 DBそのものをAIに公開しない

理想的には、

```text
execute_sql(sql)
```

だけを提供するMCPにはしません。

代わりに、

```text
search_metadata
get_metadata
get_metadata_dependencies
get_java_mapping
search_metadata_by_java
```

など、

**開発者が行いたい操作を意味のあるMCP Toolとして定義する**

方向を検討しています。

ただしPoC段階で汎用SELECT Toolを利用する合理的な理由がある場合は、そのメリット・デメリットを整理してください。

---

## 5.3 MCP Serverをセキュリティ境界とする

MCP Serverでは最低限、

- 実行可能処理の制限
- DBアクセス範囲の制限
- SQL Injection対策
- Bind Variable利用
- 最大取得件数
- Timeout
- エラー情報制御
- ログ
- 将来的な認証・認可

などを考慮します。

---

## 5.4 Oracle側でも権限を制限する

MCP Serverの実装だけに依存せず、

```text
MCP専用DBユーザー
+
必要なMetadataへのRead Only権限
```

という構成を基本とします。

---

## 5.5 最初から大規模な仕組みにしない

現時点ではPoCです。

最初から、

- Vector Database
- Embedding
- RAG基盤
- 大規模Knowledge Base
- 複雑なAgent
- 大量のMCP Tools

などを導入することは目的としていません。

Oracle Metadataは既に構造化されているため、

```text
Oracle
  ↓
必要なMetadata取得
  ↓
構造化データ
  ↓
MCP
  ↓
Copilot
```

というシンプルな仕組みから開始することを想定しています。

---

# 6. PoCで検証したいこと

PoCの最大の目的は、

**GitHub CopilotにMetadataへのアクセス手段を与えることで、本当に開発支援能力が向上するか**

を確認することです。

単に、

```text
MCPからOracleへ接続できた
```

だけではPoC成功とは考えません。

たとえば以下を確認します。

```text
1. Copilotが適切なMCP Toolを選択できるか

2. Metadataを取得できるか

3. Metadataの意味を理解できるか

4. JavaとMetadataを関連付けられるか

5. 開発者の自然言語による質問から適切な調査ができるか

6. 従来より調査時間を短縮できるか

7. 誤ったMetadata解釈をどの程度抑えられるか
```

---

# 7. 今回あなたに行ってほしいこと

まず、現在のリポジトリ全体を確認してください。

MCP Serverに関連する、

- package.json
- MCP設定
- Server実装
- Tool定義
- Oracle接続処理
- SQL
- DB接続設定
- Environment Variables
- VS Code設定
- Copilot関連設定
- README
- その他関連ファイル

を確認してください。

ファイル名は上記と異なる可能性があるため、実際のリポジトリ構成から判断してください。

そのうえで以下を実施してください。

---

# 8. Step 1：現在の実装を説明する

現在のMCP実装について、

```text
VS Code
↓
GitHub Copilot
↓
MCP Client
↓
MCP Server
↓
Oracle
```

のどこがどのように実装されているのか整理してください。

可能であればASCII図で示してください。

また、

- MCP Transport
- MCP Tools
- Resources
- Prompts
- Oracle Driver
- Thin / Thick
- Connection方式
- SQL実行方式
- Read Only制御
- エラー処理
- ログ

などについて、確認できる範囲で整理してください。

---

# 9. Step 2：現在できることを整理する

現在の実装で、

**実際にできること**

を列挙してください。

推測ではなく、コードから確認できたことを優先してください。

それぞれ、

```text
確認済み
推測
未確認
```

を区別してください。

---

# 10. Step 3：目標との差分を分析する

このプロンプトに記載した目標と、現在の実装を比較してください。

以下の形式で整理してください。

| 項目 | 現状 | 目標 | Gap | 優先度 |
|---|---|---|---|---|

特に、

- MCP Tool設計
- Oracleアクセス方式
- Security
- Metadata取得方法
- Javaとの関連付け
- Context設計
- Copilotからの使いやすさ
- メンテナンス性
- 将来的なチーム利用

を確認してください。

---

# 11. Step 4：過剰設計も検出する

不足だけでなく、

**PoCとして不要に複雑になっている箇所**

も指摘してください。

例えば、

```text
PoC段階では不要な抽象化

不要なライブラリ

過剰なレイヤー

不要なRAG

不要なVector DB

大量すぎるTool

複雑すぎる認証

不要なFramework
```

などです。

「高度だから良い」ではなく、

**PoCとして必要十分か**

という観点で評価してください。

---

# 12. Step 5：推奨アーキテクチャを提案する

現在の実装を最大限活用する前提で、

このPoCに適した構成を提案してください。

以下を分けてください。

### A. 今すぐ残すもの

現在の実装で、そのまま利用すべきもの。

### B. 修正するもの

現在の実装を活かしつつ変更すべきもの。

### C. 削除・簡略化するもの

PoCには不要なもの。

### D. 新しく追加するもの

目標達成のため不足しているもの。

---

# 13. Step 6：最小PoCを提案する

最初のPoCとして、

**最も小さい構成**

を提案してください。

目安としてMCP Toolは3〜5個程度から検討してください。

たとえば、

```text
search_metadata

get_metadata

get_java_mapping

search_metadata_by_java
```

などが候補ですが、現在のMetadata構造を確認して適切なToolを提案してください。

各Toolについて、

```text
Tool名

目的

Input

Output

実行するDB処理

Copilotが使用する具体例
```

を示してください。

---

# 14. Step 7：セキュリティレビュー

以下を確認してください。

```text
DB Credential管理

Oracleユーザー権限

SQL Injection

SELECT以外の実行可能性

Oracle Function等を経由した副作用

DB Dictionaryアクセス

別Schemaアクセス

DB Link

最大取得件数

巨大SQL

Timeout

Connection管理

エラー情報漏洩

ログへの機密情報出力

MCP Toolの入力値

MCP Toolの戻り値

Copilotへ送信される情報
```

問題がある場合は、

```text
重大
高
中
低
```

で分類してください。

---

# 15. 非機能面も確認する

以下についてもPoCとして問題がないか確認してください。

- レスポンス速度
- Oracle Connection管理
- Connection Pool
- 同時利用
- タイムアウト
- 大量データ取得
- ログ
- メンテナンス性
- Tool追加の容易さ
- 開発者PCへの展開方法

ただし、本番レベルの過剰設計は避けてください。

---

# 16. 重要な制約

現時点では、

**コードを変更しないでください。**

まず、

```text
現状把握
↓
問題点
↓
目標との差分
↓
推奨構成
↓
PoC範囲
```

を提示してください。

私が方針を確認した後で、実装変更を行います。

また、分からないことを推測で補完しないでください。

判断できない場合は、

```text
未確認
```

と明示してください。

推測する場合は、

```text
推測
```

と明示してください。

---

# 17. 最終回答形式

以下の順番で回答してください。

## 1. 現在のアーキテクチャ

ASCII図を含めて説明。

## 2. 現在できること

コードから確認できる機能。

## 3. 良い点

現在の実装で維持したい設計。

## 4. 問題点

技術・設計・セキュリティ・運用上の問題。

## 5. 目標との差分

表形式。

## 6. 推奨PoCアーキテクチャ

ASCII図を含める。

## 7. 推奨MCP Tools

3〜5個程度。

## 8. セキュリティレビュー

重大度付き。

## 9. PoCではやらない方がよいこと

過剰設計防止。

## 10. 次に実装すべきこと

優先順位順。

---

# 最も重要な判断基準

このPoCの目的は、

**「OracleにMCPで接続すること」ではありません。**

目的は、

**「GitHub Copilotが、このMetadata-Driven Application固有の知識を適切に取得し、JavaコードとMetadataを組み合わせて開発支援できることを証明する」**

ことです。

すべての設計判断を、この目的に照らして評価してください。
