# GitHub Copilot Repository Onboarding

このリポジトリで今後GitHub Copilotを使って継続的に開発します。

最初のタスクとして、このリポジトリを調査し、GitHub Copilotが安全かつ効率的に開発できるようにするための

`.github/copilot-instructions.md`

を作成してください。

## 目的

このファイルは、今後Copilotがこのリポジトリで作業するときに必要となる「リポジトリ全体で常に有効な最低限の開発ルールとコンテキスト」を提供するためのものです。

大量のルールを作ることが目的ではありません。

Copilotがコードを変更するときに、

* プロジェクトの構造を理解できる
* 既存の設計・実装パターンを尊重できる
* 正しいビルド・テスト・検証方法を選べる
* 不要な変更や推測による実装を避けられる

状態にすることを優先してください。

## Step 1: リポジトリを調査する

ファイルを作成する前に、まずリポジトリ全体を調査してください。

特に以下を確認してください。

* READMEや既存ドキュメント
* ディレクトリ構成
* 使用言語
* フレームワーク
* ライブラリ・依存関係
* package/build設定
* 実行方法
* build方法
* test方法
* lint/format方法
* 設定ファイル
* 環境変数
* CI/CD設定
* GitHub Actions
* 既存コードの命名規則
* 既存コードの設計・実装パターン
* エラーハンドリング
* ロギング
* テストコードの配置と書き方
* セキュリティ上注意すべき箇所

ファイル名だけで判断せず、必要なファイルの内容を確認してください。

## Step 2: 事実と推測を分離する

リポジトリから確認できた内容だけをInstructionsに記載してください。

不明な事項を一般的なベストプラクティスで勝手に補完しないでください。

確認できない内容については、

* 推測してInstructionsへ追加しない
* 必要なら私に質問する
* 今後決めるべき事項として報告する

のいずれかにしてください。

既存コードと一般的なベストプラクティスが異なる場合は、勝手に大規模な変更方針を導入せず、まず既存方式を把握してください。

## Step 3: Instructionsを設計する

`.github/copilot-instructions.md` には、リポジトリ全体で常に必要になる情報だけを記載してください。

例えば以下です。

* Project Overview
* Technology Stack
* Repository Structure
* Architecture / Design Principles
* Coding Conventions
* Build / Run / Test
* Change Guidelines
* Security / Safety Constraints

ただし、実際のリポジトリに不要なセクションは作らなくて構いません。

## 重要な設計原則

Instructionsは簡潔にしてください。

長い説明よりも、Copilotが実際の作業時に判断できる具体的な指示を優先してください。

既存コードから読み取れる細かな情報を大量にコピーしないでください。

特定の言語・ディレクトリだけに関係する詳細ルールが多い場合は、すべてを `copilot-instructions.md` に入れず、

`.github/instructions/*.instructions.md`

へ分離すべきか検討してください。

特定タスク専用の長い手順についても、常時Instructionsへ入れないでください。

## Copilotの作業原則

今後このリポジトリで変更を行う際は、原則として以下を守るようInstructionsへ反映してください。

* 変更前に関連コードを確認する
* 既存の設計・命名・実装パターンを優先する
* 必要最小限の変更を行う
* 関係のないリファクタリングを同時に行わない
* 新しい依存ライブラリを安易に追加しない
* APIやライブラリの仕様を推測しない
* 既存のテスト方法がある場合はそれを使用する
* 変更後は可能な範囲でbuild/test/lintを実行する
* テストを通すために既存テストを安易に削除・弱体化しない
* 秘密情報、認証情報、個人情報をコードやログへ追加しない

## Step 4: 作成前に自己レビューする

Instructionsを作成する前に、内容を確認してください。

特に以下を確認してください。

1. リポジトリから確認できない内容を事実として書いていないか
2. 既存コードと矛盾するルールを追加していないか
3. Instructionsが過剰に長くなっていないか
4. リポジトリ全体に適用する必要のないルールが混ざっていないか
5. Copilotが実際の開発時に判断しやすい具体的な内容になっているか

問題があれば修正してください。

## Step 5: ファイルを作成する

最終的に

`.github/copilot-instructions.md`

を作成してください。

`.github` ディレクトリが存在しない場合は作成してください。

既存の `copilot-instructions.md` が存在する場合は上書きせず、内容を確認して改善案を提示してください。

## 完了報告

作成後、チャットでは簡潔に以下を報告してください。

* 調査した主要ファイル
* 判断した技術スタック
* Instructionsへ入れた主要ルール
* あえてInstructionsへ入れなかった事項
* 不明点
* `.github/instructions/*.instructions.md` 等へ将来分離した方がよい事項

コード実装そのものにはまだ着手しないでください。
今回のタスクはリポジトリのオンボーディングとInstructions作成までです。
