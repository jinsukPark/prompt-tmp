# Development Setup & Copilot Instructions

これまでの会話で、このプロダクトの目的・機能・技術構成・開発方針について検討してきました。

これから本格的な実装に入ります。

実装を開始する前に、これまでの会話内容と現在のリポジトリを整理し、今後GitHub Copilotが一貫した方針で開発できるように `.github/copilot-instructions.md` を作成してください。

まだ本格的な機能実装には着手しないでください。

## 1. これまでの会話を整理する

まず、これまでの会話から以下を整理してください。

* プロダクトの目的
* 解決する課題
* 想定ユーザー
* 主要機能
* MVPの範囲
* MVPでは実装しないもの
* 技術スタック
* アーキテクチャ
* データモデルに関する方針
* UI/UXに関する方針
* セキュリティ上の制約
* 開発・運用上の制約
* 私が明示的に決定した事項
* まだ決定していない事項

「決定済み」「会話から合理的に読み取れるもの」「未決定」を区別してください。

未決定事項を勝手に決定しないでください。

## 2. 現在のリポジトリを調査する

次に、現在のリポジトリを確認してください。

特に以下を確認してください。

* README / docs
* ディレクトリ構造
* 使用言語
* framework / runtime
* package / dependency
* build設定
* test設定
* lint / format設定
* 環境変数
* CI/CD
* GitHub Actions
* 既に存在するコード
* 既に存在する設計・命名規則

会話で決めた内容と現在のリポジトリに矛盾がある場合は、勝手にどちらかへ合わせず報告してください。

## 3. 開発開始前に情報を分類する

収集した情報を以下の3種類に分類してください。

### A. Product Specification

「何を作るか」に関する情報。

例:

* プロダクト目的
* ユーザー
* 機能
* MVP範囲
* ビジネスルール

### B. Architecture / Technical Decisions

「どのように作るか」に関する情報。

例:

* 技術スタック
* アーキテクチャ
* データ設計
* API設計
* 認証方式
* 外部サービス
* 技術的制約

### C. Copilot Instructions

「Copilotが開発するとき常に守るべきこと」。

例:

* 既存アーキテクチャを尊重する
* 既存の命名・実装パターンを優先する
* MVPの範囲を勝手に拡張しない
* 必要最小限の変更を行う
* 無関係なリファクタリングを行わない
* 新規依存関係を安易に追加しない
* APIやライブラリ仕様を推測しない
* セキュリティ制約を守る
* 変更後にbuild/test/lintを実行する

Product Specificationそのものを大量にCopilot Instructionsへコピーしないでください。

## 4. copilot-instructions.md を作成する

上記を踏まえて、

`.github/copilot-instructions.md`

を作成してください。

このファイルには「今後のほぼすべての開発タスクでCopilotが知っている必要がある情報」だけを記載してください。

短く、具体的で、実行可能な指示を優先してください。

必要に応じて以下のような構成を使用してください。

# Project Context

# Technology Stack

# Architecture Principles

# Coding Principles

# Change Guidelines

# Testing and Validation

# Security

ただし、不要なセクションを無理に作らないでください。

## 5. 特に守る開発原則

以下について、このプロジェクトに適切なものをInstructionsへ反映してください。

* 実装前に関連コードを確認する
* 既存の設計・命名・パターンを優先する
* MVPのスコープを勝手に拡張しない
* 要求されていない機能を追加しない
* 必要最小限の変更を行う
* unrelated refactoringを避ける
* 不要な抽象化を避ける
* 将来使うかもしれないという理由だけで機能を作らない
* 新しいライブラリを追加する前に既存手段で実現できないか確認する
* 秘密情報や認証情報をコードへ含めない
* APIやライブラリの存在・仕様を推測しない
* 不明な仕様について重要な判断を勝手に行わない
* 変更後は利用可能なbuild/test/lint/typecheckを実行する
* テストを通すためだけにテストを削除・弱体化しない

## 6. Instructionsを肥大化させない

すべてを `.github/copilot-instructions.md` に入れないでください。

以下については必要になった段階で分離することを検討してください。

* 特定ディレクトリだけのルール
* 特定言語だけのルール
* テスト専用ルール
* DB専用ルール
* セキュリティ専用ルール
* 定型的な作業手順

必要であれば将来的に

`.github/instructions/*.instructions.md`

などへ分割する案を提示してください。

ただし、現時点では必要性が明確でないファイルを大量に作成しないでください。

## 7. 自己レビュー

作成前に以下を確認してください。

1. 会話で決定していないことを勝手に決定していないか
2. 現在のリポジトリと矛盾していないか
3. Product SpecificationとCopilot Instructionsを混同していないか
4. Instructionsが過剰に長くなっていないか
5. 不要なルールを追加していないか
6. MVPを超える過剰設計を促す内容になっていないか
7. Copilotが実際の開発時に判断できる具体的な指示になっているか

問題があれば修正してください。

## 8. 完了報告

ファイル作成後、チャットで以下を報告してください。

* 会話から「決定済み」と判断した主要事項
* 未決定事項
* リポジトリから確認した主要事項
* copilot-instructions.md に入れた内容
* あえて入れなかった内容
* 会話とリポジトリの矛盾
* 今後 instructions / prompts / docs 等へ分離した方がよい内容

重大な未決定事項や矛盾があり、本格実装前に決める必要がある場合は、その点を明示してください。

このタスクでは、本格的な機能実装は開始しないでください。
